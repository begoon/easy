PROGRAM hw
    Variables:
        a: INTEGER
    Statements:
        SET a := 100
        OUTPUT('A', ||:('Ai', 'abc ', a))
        FOR a := 0 TO 10 DO
            Statements:
                OUTPUT('i', a)
        END FOR
        EXIT

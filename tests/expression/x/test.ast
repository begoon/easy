PROGRAM Expression
    Variables:
        a, b, c: INTEGER
    Statements:
        SET x := a
        SET x := (a + b)
        SET x := (+a)
        SET x := (-a)
        SET x := (a + (b * с))
        SET x := ((((-b) + (1 - 2)) - (LENGTH('3') * 7)) ^ 1)
        SET x := (((-b) + (1 - 2)) - func(3))
        SET x := (NOT(a < b))
        SET x := ||:('ii', a, b)
        SET x := ||:('si', 'a', b)
        SET x := ||:('sii', 'a', b, c)
        SET x := ||:('iA', a, ||:('iA', b, SUBSTR('abc', 0, 2)))
        SET x := ||:('iiA', a, b, SUBSTR('abc', 0, 2))
        SET x := ||:('iA', a, CHARACTER(b))

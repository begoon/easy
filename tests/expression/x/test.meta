PROGRAM Expression
    Variables:
        x, a, b, c: INTEGER
        s: STRING
        f: BOOLEAN
    Subroutines:
        FUNCTION func() : INTEGER
            Statements:
                RETURN 0
    Statements:
        SET x := a
        SET x := (a + b)
        SET x := (+a)
        SET x := (-a)
        SET x := (a + (b * c))
        SET x := (((-b) + (1 - 2)) - (LENGTH('3') * 7))
        SET x := (((-b) + (1 - 2)) - func(3))
        SET f := (NOT(a < b))
        SET f := ((a < b) XOR TRUE)
        SET s := ||:('ii', a, b)
        SET s := ||:('Ai', 'a', b)
        SET s := ||:('Aii', 'a', b, c)
        SET s := ||:('iA', a, ||:('iA', b, SUBSTR('abc', 0, 2)))
        SET s := ||:('iiA', a, b, SUBSTR('abc', 0, 2))
        SET s := ||:('iA', a, CHARACTER(b))
